---
const { lineWidth } = Astro.props;
---

<div class="line"></div>
<style define:vars={{ lineWidth }}>
  .line {
    height: 2px;
    background: var(--dark_blue);
    width: var(--lineWidth);
    margin: auto;
  }
</style>
